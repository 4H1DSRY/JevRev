# MODEL CHECKPOINT - ASTRA

Date: 2026-09-22

Completion note: this handoff's bounded implementation and verification were
completed on 2026-09-22. See `FINAL_VERIFICATION.md` and canonical `STATE.json`
for the final evidence and limitations. The checklist below is retained as the
original acceptance contract, not a new queue of unfinished work. No commit
or push was made.

This is an internal production handoff. None of its model, governance,
checkpoint, or design-process language belongs in README copy or in the final
asset.

## Checkpoint

- Repository: `C:\Users\duan\Documents\ChatGPT\1\JevRev`
- Upstream base: `a7036b4ff289f88382bd93bb465da9f55570bd70`
- Feature branch: `codex/docs-flagship-case`
- Product boundary: `Sift -> bounded probes -> evidence -> Decide`
- Remaining visual tasks:
  1. create and integrate `.github/assets/jevrev-decision-gate.svg`;
  2. add the bounded advanced interaction and visual-anchor pass to the
     JevRev-routed page described below;
  3. capture the enhanced routed workflow state, integrate it into README, and
     perform the final GitHub-rendered aesthetic and layout audit.
- Direct page work: only a clearly verified presentation defect. Do not redesign
  the direct page or replace its recorded direction.
- Routed page work: the advanced pass below is explicitly authorized, but it
  must evolve the selected mechanism rather than replace it.
- No commit or push has been made.

## Completed visual system

The one-shot comparison has completed its second recorded evolution.

### Direct condition

- File: `benchmarks/one-shot-showcase/direct/index.html`
- Direction: preserved conventional rounded SaaS grammar, centered system-sans
  hierarchy, green actions, light neutral surfaces, and soft elevation.
- Ambient treatment: a restrained purple-black transition is limited to the
  page light source. It does not turn the light theme into a dark page.
- Asset rule: the direct page is image-free. Do not restore the banner or skull.
- Purpose: remain a credible polished first landing-page direction rather than
  a caricature created to exaggerate the comparison.

### JevRev-routed condition

- File: `benchmarks/one-shot-showcase/routed/index.html`
- Selected mechanism: `painterly-evidence-dossier`
- Deterministic Sift score: `0.9149`
- Direction input: `benchmarks/one-shot-showcase/sift-input.json`
- Replay: `benchmarks/one-shot-showcase/sift-replay.json`
- Frozen content and behavior contract:
  `benchmarks/one-shot-showcase/input.json`
- Browser record: `benchmarks/one-shot-showcase/VALIDATION.md`

The routed page deliberately combines two layers:

1. Elegant literary case-file expression: serif display type, paper or ink
   surfaces, restrained rust accent, the existing hand-drawn skull.
2. Hard modern engineering structure: sharp borders, strict evidence grids,
   monospace measurements, keyboard decision tabs, command block, and explicit
   candidate routes.

Do not let either layer erase the other. The target is cool and elegant, not a
generic SaaS page and not a pure retro editorial page.

The negative skull treatment is a protected anchor:

- Source: `.github/assets/jevrev-banner.png`
- Derived transparent line art: `.github/assets/jevrev-skull-negative.png`
- The transparent asset shares the page background. It must not regain a black
  rectangular image background.
- Dark mode keeps white negative line work. Light mode uses the same line art in
  ink so it remains legible.
- Red paint, outline glow, or decorative scribble over the skull is forbidden.
- A faint rust-colored ambient source behind the skull's upper-right field is
  intentional. Preserve it as page light only. It must not become a mark,
  outline, flare, or overlay on the line art.
- Do not add an image-overlay label. The skull should carry its own weight.

Functional routed interactions are complete:

- keyboard-accessible `Sift / Probe / Decide` stage rail;
- arrow-key tab movement;
- proposal-versus-evidence comparison lens;
- expandable source trail;
- working copy-install action;
- IntersectionObserver reveal with reduced-motion fallback;
- light, dark, desktop, and mobile layouts.

## Routed advanced interaction pass

Reading this as: a preservation redesign of a developer-tool evidence dossier
for technical readers, with an industrial editorial language leaning on native
CSS and JavaScript rather than a component framework.

- `DESIGN_VARIANCE: 7`
- `MOTION_INTENSITY: 5`
- `VISUAL_DENSITY: 7`

The objective is not more animation. It is denser causal reading and clearer
state change. Implement a small coherent system with these three required
effects:

1. **Semantic stage path.** Turn the existing Sift, Probe, and Decide rail into a
   visible route. The active segment and its state mark must show where evidence
   is in the process. Connectors are allowed only because they encode sequence.
2. **Motivated stage transition.** When a stage changes, the question, body, and
   fact group should transition as one state change using only transform and
   opacity. The animation communicates progression and hierarchy. It must not
   become a repeated entrance spectacle.
3. **Proposal-to-evidence reversal.** Make the lens change visibly communicate
   that the ordering changed after evidence. Candidate position, route, state,
   and decision stamp may transition together. If visual order changes, DOM,
   reading, and focus order must change with it. The live region must announce
   a concise outcome without exposing internal process text.

Use three deliberate visual anchors:

1. the protected negative skull with its faint rust source behind the
   upper-right field;
2. the active stage field, including the semantic route and current `S`, `P`, or
   `D` state mark;
3. the evidence winner and final decision stamp after the lens changes.

Optional micro-polish is allowed for focus, hover, disclosure, and copy feedback
when it strengthens affordance. Do not add perpetual loops, parallax, pointer
tracking, magnetic controls, custom cursors, decorative particles, synthetic
metrics, ornamental status dots, or another image asset. Add no dependency.

All motion must stop under `prefers-reduced-motion: reduce`, with every state and
fact still immediately available. Preserve native keyboard operation, visible
focus, `aria-selected`, `aria-pressed`, the tabpanel relationship, the live
region, and the native details disclosure. Test light and dark modes at desktop
and mobile widths even though mobile screenshots will not appear in README.

Do not change shared copy merely to create animation hooks. Small accessible
status text is allowed only when it states an already verified route or result.

Six committed captures remain available as validation evidence:

- `.github/assets/one-shot-showcase/direct-hero.png`
- `.github/assets/one-shot-showcase/direct-structure.png`
- `.github/assets/one-shot-showcase/direct-mobile.png`
- `.github/assets/one-shot-showcase/routed-hero.png`
- `.github/assets/one-shot-showcase/routed-decision.png`
- `.github/assets/one-shot-showcase/routed-mobile.png`

The two mobile captures remain committed for browser evidence but are no longer
displayed in README. The current public comparison displays four captures:

- `direct-hero.png` and `routed-hero.png` as the paired overview;
- `direct-structure.png` and `routed-decision.png` as the collapsed structural
  and evidence-state pair.

After the advanced pass, add this fifth public capture:

- `.github/assets/one-shot-showcase/routed-workflow.png`

Capture the real workflow section at a desktop or medium width with `Probe`
selected so the semantic stage path, state mark, question, and evidence facts
are all visible. Use a full-width element crop with no browser chrome, synthetic
composite, callout overlay, or decorative caption inside the pixels. Refresh
`routed-decision.png` in the evidence state if the reversal treatment changes.

The routed hero screenshot intentionally uses dark mode so the protected
negative treatment is visible in README. The comparison uses four forms of
evidence without moving setup or the primary engineering case:

1. a paired hero overview with short material captions;
2. a six-row decision-to-output trace covering route, hierarchy, material,
   proof behavior, asset policy, and narrow-width behavior;
3. an expandable visual audit with paired structure or decision screenshots and
   one full-width enhanced workflow state;
4. a compact interaction contract covering input, information change, and the
   reduced-motion behavior of each control.

The old single comparison PNG and comparison HTML were removed.

## Remaining SVG objective

Create a compact engineering decision diagram inside `## A faster shortcut
lost`. It must explain the ranking reversal in one glance before a reader reaches
the detailed protocol.

Required order:

```text
engineering problem
        -> two plausible paths
        -> equal probe contract
        -> evidence diverges
        -> regex rejected / state machine ready
        -> Decide winner
```

Required facts:

1. Sift chose what was worth testing, not what was already proven.
2. Both candidates received the same correctness and repeated benchmark
   requirements.
3. The proposal favorite was faster locally but failed correctness.
4. The lower-ranked path passed correctness and became the evidence winner.
5. The final route is `winner -> integrate_winner`.

Verified values from the executed delimiter-counter case:

- `regex-shortcut`
  - Sift score: `0.9593`
  - latest local throughput: approximately `16.34x` baseline
  - correctness: 2 passed, 3 failed out of 5
  - evidence state: rejected
- `indexed-state-machine`
  - Sift score: `0.8773`
  - latest local throughput: approximately `4.75x` baseline
  - correctness: 5 passed out of 5
  - Decide score: `0.928`
  - evidence state before Decide: ready
  - final outcome: winner
  - next action: `integrate_winner`
- Each candidate had 2 command records, 1 metric group, 1 implementation
  artifact, 7 baseline samples, and 7 candidate samples.
- Throughput ratios are machine-dependent secondary detail. The stable fact is
  the correctness-driven reversal.

## SVG visual direction

- `DESIGN_VARIANCE: 5`
- `MOTION_INTENSITY: 1`
- `VISUAL_DENSITY: 6`
- Static, responsive, dependency-free SVG for GitHub Markdown.
- Precise editorial engineering diagram, primarily monochrome.
- Use pass and fail accents only when text or shape also carries the meaning.
- Complement the banner and the one-shot evidence system without duplicating
  the skull or becoming another landing-page screenshot.
- Preserve the current README banner, title, tagline, badges, and warm black-ink
  identity.
- Avoid dashboards, card walls, gradients, glass, neon, gauges, stock icons,
  decorative animation, and giant performance numerals.

Suggested concise labels:

- `SIFT FAVORITE`
- `SAME PROBE CONTRACT`
- `FASTER LOCALLY`
- `CORRECTNESS FAILED`
- `REJECTED`
- `CORRECTNESS PASSED`
- `EVIDENCE WINNER`
- `INTEGRATE WINNER`

## README integration point

Inside `## A faster shortcut lost`, replace only this temporary block:

```text
proposal favorite                         evidence decision

regex-shortcut          -> faster locally -> correctness failed -> rejected
indexed-state-machine   -> faster locally -> correctness passed -> winner
```

with:

```markdown
![Two equally probed delimiter-counter candidates diverge on correctness, reversing the Sift ranking at Decide.](.github/assets/jevrev-decision-gate.svg)
```

Keep the section heading, the two setup sentences, the conclusion beginning
`The regex was faster and wrong`, the `run-the-case` anchor, and the
reproducibility command unchanged unless the rendered SVG exposes a factual
conflict.

Do not relocate or broadly rewrite `## One brief, two one-shot pages`. Preserve
the paired heroes, decision-to-output trace, source links, interaction contract,
and limitation paragraph unless the rendered audit exposes a specific defect.
Insert the new full-width `routed-workflow.png` inside the existing details block
after the structure or decision pair and before the interaction contract.

## README aesthetic and layout verification

This is Astra's second and final responsibility after the decision-gate SVG.
It is a rendering and micro-polish pass, not a new design cycle. Astra also has
bounded creative discretion to make one to three small, high-leverage
interventions where the README is not already fact-locked.

Target expression:

- extreme industrial precision with premium editorial restraint;
- high information density without dashboard styling;
- clear causal reading from decision to visible consequence;
- image rhythm strong enough to feel designed, but compact enough that the
  engineering ranking reversal and setup path remain primary;
- no decorative status dots, fake scores, progress tracks, ornamental labels,
  or unsupported superiority language.

Required README checks:

1. Inspect the rendered section on GitHub-like light and dark canvases at a
   normal desktop width and near 360 CSS pixels.
2. Verify that the direct and routed hero images receive equal visual weight,
   preserve their native aspect ratio, and do not appear accidentally cropped.
3. Verify that the six-row trace reads as a causal comparison rather than a
   feature checklist, and that wrapping does not make the table illegible.
4. Expand the details block and inspect the structure or decision pair, the
   full-width routed workflow state, and the interaction contract for rhythm,
   alignment, caption clarity, and excessive height.
5. Confirm every image loads by repository-relative path and has specific alt
   text that describes what is visible rather than making a quality claim.
6. Confirm the section remains compact when the details block is closed and
   does not displace installation or later protocol documentation.
7. If a defect is verified, make only the smallest change to widths, ordering,
   captions, or spacing needed to correct it. Do not alter experiment facts,
   page direction, frozen copy, or evidence limits.

Return the exact widths and contexts inspected, the defect list or explicit
no-defect result, and any changed README lines for factual review.

### Bounded high-leverage discretion

Do not bury Astra's design judgment inside pure compliance work. After the
required audit, identify at most three small interventions that could improve
the whole reading experience disproportionately to their size. Astra may
implement the strongest ones directly when they pass the constraints below.

Promising leverage points include:

- establish one shared evidence vocabulary across the decision-gate SVG and
  comparison section, such as proposal, observed evidence, and route, so the
  README feels like one authored system rather than adjacent artifacts;
- improve the visual hinge between the ranking-reversal proof, setup, and the
  one-shot comparison with a concise native-Markdown transition, caption, or
  alignment adjustment;
- make the collapsed comparison summary carry more information before it opens,
  or improve the rhythm and order of its paired evidence after expansion;
- replace a fragile table detail with a more legible GitHub-native structure if
  narrow rendering proves materially better, while preserving compactness;
- introduce one restrained typographic or rule-based motif that links the SVG,
  evidence trace, and captions without becoming ornament or a new component
  system;
- improve one global README hierarchy detail outside the comparison if it
  shortens the path from verified proof to action without moving or renaming a
  stable section.

Every intervention must satisfy all of these conditions:

1. It is small enough to review as an isolated diff and easy to revert.
2. It solves a demonstrated hierarchy, rhythm, scanning, or narrow-width issue,
   or creates a clear shared visual grammar across existing proof artifacts.
3. It uses GitHub-native Markdown, HTML, or the dependency-free SVG. No remote
   assets, scripts, custom CSS, or renderer-specific hacks.
4. It adds no new factual claim, score, decorative status, or process language.
5. It does not push setup materially lower, enlarge the comparison into the
   primary proof, or turn the README into a landing page.
6. It preserves the banner, title, tagline, badges, stable headings and anchors,
   product boundary, roadmap labels, reproduction paths, and limitation copy.
7. It does not redesign either one-shot page. The only permitted page change is
   the explicitly scoped routed interaction pass or correction of a verified
   presentation defect.

If none of the candidate interventions clearly earns its space, report that and
leave the structure unchanged. Restraint is a valid design decision, but Astra
must explicitly evaluate the leverage points rather than skipping them.

## Forbidden claims and leakage

Do not claim or imply:

- zero total cost, free inference, negligible elapsed time, or universal low
  latency;
- production defect reduction, engineering hours saved, token savings,
  conversion lift, or general design-quality improvement;
- that JevRev authored implementations, created worktrees, ran arbitrary
  commands, merged code, or integrated the winner;
- that JevLoop or JevLong or JevKanban ships in the current release;
- that Sift scores are proof or local throughput generalizes;
- that the controlled one-shot showcase is a neutral or live-provider
  benchmark;
- internal prompts, governance, model switching, handoff, or production intent.

No em dash or en dash may appear in visible README, HTML, or SVG text.

## SVG acceptance

- Include a specific `<title>` and `<desc>`.
- Use a rational `viewBox` with no fixed-size dependency.
- Use no JavaScript, external fonts, remote images, or external stylesheets.
- Keep important labels as real SVG text when practical.
- Remain readable on GitHub light and dark surfaces and near 360 CSS pixels.
- Carry no meaning by color alone.
- Prevent clipping, overflow, tiny labels, or excessive empty canvas.
- Render and inspect the actual SVG at original resolution.
- Validate XML and repository-relative loading.

Required visual checks:

1. GitHub-like light background at desktop width.
2. GitHub-like dark background at desktop width.
3. Narrow presentation near 360 pixels.
4. Original-resolution alignment and clipping inspection.

## Current verification state

Before the final SVG, the evolved one-shot fixture passes:

- deterministic Sift selection of `painterly-evidence-dossier`;
- content and behavior contract validation;
- desktop and mobile overflow checks;
- light and dark rendering;
- reduced-motion content visibility;
- decision-tab mouse and keyboard states;
- proposal and evidence lens states;
- anchors, evidence disclosure, local links, and copy action;
- fresh console with zero errors and zero warnings.
- direct primary and copy-control contrast measured 5.26:1 in light mode and
  7.85:1 in dark mode;
- the routed upper-right rust source was visually inspected at desktop and
  mobile widths in both themes and remains behind the transparent line art.

The README no longer displays paired mobile screenshots. They remain committed
for verification. A GitHub-native interaction contract now occupies that role
until the enhanced full-width `routed-workflow.png` is captured and inserted.

The direct condition was subsequently restored to its approved rounded visual
grammar and its desktop, structure, and mobile evidence was regenerated. After
that correction, rerun the complete verification list below rather than relying
on the earlier suite result.

After the direct correction, the repository passed `npm run check`, 13 test
files with 106 tests, `npm run build`, `npm run demo:all`, `npm run
demo:workflow`, `npm run demo:oneshot`, and `npm pack --dry-run` with 130 files
at approximately 144.3 KB. The governance audit was rerun after this handoff
update with no errors or warnings and must run again after the SVG.

After adding the accepted rust ambient source, `npm run demo:oneshot`, the full
browser matrix, `git diff --check`, and the governance audit passed again. The
change is CSS-only and does not alter the image asset or fixture contract.

## Final verification after SVG

- render and inspect the SVG in all required contexts;
- check README hierarchy, the decision-to-output trace, the collapsed image
  pair, the full-width workflow state, and interaction contract in all required
  contexts;
- confirm no em dash, en dash, or internal-process leakage in public files;
- run `git diff --check`;
- run `npm run check`;
- run `npm test`;
- run `npm run build`;
- run `npm run demo:all`;
- run `npm run demo:workflow`;
- run `npm run demo:oneshot`;
- run `npm pack --dry-run`;
- run the product-governance audit;
- fetch `origin/main` and reconcile only if upstream changes invalidate facts.

## Return condition

Return for factual review only when:

- `.github/assets/jevrev-decision-gate.svg` exists and replaces only the
  temporary text proof;
- the evolved one-shot fixture and all six validation captures remain intact;
- `routed-workflow.png` exists, shows a real enhanced workflow state, and is the
  only new screenshot added to the public README;
- the public README contains no paired mobile screenshot block;
- the transparent negative skull has no red overlay or mismatched image box;
- the SVG passes accessibility and cross-context rendering checks;
- the correctness-driven reversal remains dominant over machine-specific
  numbers;
- the README rendering audit reports exact contexts and either a no-defect
  result or only minimal, reviewable micro-polish;
- public files contain no forbidden claim or internal production language;
- final verification evidence and remaining caveats are recorded in
  `STATE.json`.
