# JevRev protocol compatibility note

Use [`docs/PROTOCOL.md`](../../../docs/PROTOCOL.md) as the source of truth.
The old invocation `specjev rank` is accepted as an alias for
`jevrev run`; new callers should use `jevrev run --format json`.
